# Backend Server Port
PORT=5000

# Environment Mode
NODE_ENV=development

# Optional MongoDB Connection String
# If provided, the backend automatically connects to MongoDB instead of local SQLite.
# MONGODB_URI=mongodb://localhost:27017/money_manager

